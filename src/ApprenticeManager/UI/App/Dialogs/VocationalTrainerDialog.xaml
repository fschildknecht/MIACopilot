<Window x:Class="ApprenticeManager.UI.App.Dialogs.VocationalTrainerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lehrlingsbetreuer" Height="380" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Vorname *" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="FirstNameBox" Grid.Row="0" Grid.Column="1" Margin="0,5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Nachname *" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="LastNameBox" Grid.Row="1" Grid.Column="1" Margin="0,5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Betrieb *" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="CompanyBox" Grid.Row="2" Grid.Column="1" Margin="0,5"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="E-Mail" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="EmailBox" Grid.Row="3" Grid.Column="1" Margin="0,5"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Telefon" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="PhoneBox" Grid.Row="4" Grid.Column="1" Margin="0,5"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Bemerkungen" VerticalAlignment="Top" Margin="0,8,0,0"/>
                <TextBox x:Name="NotesBox" Grid.Row="5" Grid.Column="1" Margin="0,5"
                         Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Speichern" Width="90" Margin="0,0,8,0" Click="OkButton_Click" IsDefault="True"/>
            <Button Content="Abbrechen" Width="90" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
